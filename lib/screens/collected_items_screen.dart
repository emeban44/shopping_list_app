import 'package:flutter/material.dart';

class CollectedItemsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Collected'),
    );
  }
}
